<%= render partial: 'og_meta_tags', locals: { page: @page } %>

<% cache([:v3, @page]) do %>
  <center><h1><%= @page.title %></h1></center>
  <br>
  <div id='page_content'>
    <%= raw @page.body %>
  </div>
<% end %>